# LLMQuiz

This is a Python FastAPI project that solves data-analysis quizzes automatically using a headless browser and LLM logic.

## Setup

1. Install dependencies:

```bash
pip install -r requirements.txt
playwright install
